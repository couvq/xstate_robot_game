import type { Direction } from "./types";

export const BOARD_SIZE = 5;
export const INITIAL_SCORE = 0;
export const GAME_TIME_SECS = 30;
export const positions: Direction[] = ["up", "down", "left", "right"];
